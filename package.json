{
  "name": "movie-database",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.3.2",
    "vite-plugin-pug": "^0.3.2"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
